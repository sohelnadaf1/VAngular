import { SalaryupdatePipe } from './salaryupdate.pipe';

describe('SalaryupdatePipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryupdatePipe();
    expect(pipe).toBeTruthy();
  });
});
